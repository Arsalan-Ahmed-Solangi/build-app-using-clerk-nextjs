import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#111827",
    },
    background: {
      default: "#f8fafc",
    },
  },
  shape: {
    borderRadius: 10,
  },
});

export default theme;
